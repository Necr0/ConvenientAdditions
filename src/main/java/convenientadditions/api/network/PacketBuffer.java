package convenientadditions.api.network;

import java.util.ArrayList;
import java.util.List;

import net.minecraft.network.Packet;

public class PacketBuffer {
	/*public ArrayList<Packet> packets=new ArrayList<Packet>();
	
	public void schedulePackage(Packet packet,boolean override){
		if(override){
			Class c=packet.getClass();
			for(Packet p:packets){
				if(c==p.getClass())
					packets.remove(p);
			}
		}
		packets.add(packet);
	}
	
	public void push(){
		
	}
	
	public int getLength(){
		return packets.size();
	}
	
	public List<Packet> getPackets(){
		return packets;
	}*/
}
